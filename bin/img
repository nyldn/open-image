#!/usr/bin/env node
const [major] = process.versions.node.split(".").map(Number);
if (major < 18) {
  console.error(`img requires Node 18+, found ${process.versions.node}`);
  process.exit(1);
}

const { main } = await import("../src/img.mjs");

await main(process.argv.slice(2));
